<ContentDialog
    x:Class="Cerosoft.AirPoint.Server.SettingsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    CornerRadius="16"
    BorderThickness="0"
    MaxWidth="500"
    Shadow="{x:Null}"
    Style="{StaticResource DefaultContentDialogStyle}">

    <ContentDialog.Background>
        <SolidColorBrush Color="Transparent"/>
    </ContentDialog.Background>

    <Grid Width="400" Background="#000000" CornerRadius="16" BorderBrush="#333333" BorderThickness="1">

        <Border Width="400" Height="400" CornerRadius="200" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-200,-150,0" 
                Opacity="0.15" IsHitTestVisible="False">
            <Border.Background>
                <RadialGradientBrush>
                    <GradientStop Color="#00E676" Offset="0"/>
                    <GradientStop Color="Transparent" Offset="1"/>
                </RadialGradientBrush>
            </Border.Background>
        </Border>

        <Grid Padding="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,24">
                <TextBlock Text="Settings" FontSize="20" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text="Configure connection and behavior" FontSize="12" Foreground="#888888" Margin="0,4,0,0"/>
            </StackPanel>

            <StackPanel Grid.Row="1" Spacing="16">
                <TextBlock Text="Connection Mode" FontWeight="SemiBold" Foreground="#00E676" FontSize="14"/>
                <StackPanel Spacing="8">
                    <RadioButton x:Name="WifiRadio" GroupName="Conn" Foreground="White">
                        <StackPanel>
                            <TextBlock Text="Wi-Fi / Hotspot" Foreground="White"/>
                            <TextBlock Text="Recommended for high speed" Foreground="#666" FontSize="11"/>
                        </StackPanel>
                    </RadioButton>
                    <RadioButton x:Name="BluetoothRadio" GroupName="Conn" Foreground="White">
                        <StackPanel>
                            <TextBlock Text="Bluetooth Mode" Foreground="White"/>
                            <TextBlock Text="Does not require Wi-Fi" Foreground="#666" FontSize="11"/>
                        </StackPanel>
                    </RadioButton>
                </StackPanel>
            </StackPanel>

            <Rectangle Grid.Row="2" Height="1" Fill="#22FFFFFF" Margin="0,20"/>

            <StackPanel Grid.Row="3" Spacing="16">
                <TextBlock Text="System Behavior" FontWeight="SemiBold" Foreground="#00E676" FontSize="14"/>

                <ToggleSwitch x:Name="StartupToggle" Foreground="White" OnContent="On" OffContent="Off">
                    <ToggleSwitch.Header>
                        <StackPanel>
                            <TextBlock Text="Run at Windows Startup" Foreground="White"/>
                            <TextBlock Text="Launch automatically when you sign in" Foreground="#666" FontSize="11" Margin="0,2,0,0"/>
                        </StackPanel>
                    </ToggleSwitch.Header>
                </ToggleSwitch>

                <ToggleSwitch x:Name="TrayToggle" Foreground="White" OnContent="On" OffContent="Off">
                    <ToggleSwitch.Header>
                        <StackPanel>
                            <TextBlock Text="Minimize to System Tray" Foreground="White"/>
                            <TextBlock Text="Run in background with a tray icon" Foreground="#666" FontSize="11" Margin="0,2,0,0"/>
                        </StackPanel>
                    </ToggleSwitch.Header>
                </ToggleSwitch>

                <StackPanel Margin="0,20,0,0" Spacing="4" HorizontalAlignment="Center" Opacity="0.5">
                    <TextBlock Text="Cerosoft AirPoint v1.0.0" Foreground="#888" FontSize="11" HorizontalAlignment="Center"/>
                    <TextBlock Text="© 2026 Cerosoft" Foreground="#666" FontSize="10" HorizontalAlignment="Center"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Spacing="10" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button Content="Cancel" Click="Cancel_Click" Background="Transparent" Foreground="#AAA" BorderBrush="#333333" Width="80"/>
                    <Button Content="Save" Click="Save_Click" Background="#00E676" Foreground="Black" FontWeight="Bold" BorderThickness="0" CornerRadius="4" Width="80"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</ContentDialog>